#include <stdio.h>

int main(){
    int i; // incia a variável de controle do loop
    for(i=0; i<10; i++){
        printf("i = %d\n", i);
    }
    // i = 0 inicia a variável i com 0
    // i<10 é a condição de parada do loop
    // i++ incrementa a variável i em 1 a cada iteração

    return 0;
}